- content_for :menu do
  - if user_signed_in?
    .navbar-collapse.collapse.navbar-responsive-collapse
      %ul.nav.navbar-nav
        %li{:class => (s == "info" ? "active" : "")}= link_to "Informacje", home_path
        %li{:class => (s == "matches" ? "active" : "")}= link_to "Mecze", rounds_path
        %li{:class => (s == "ranking" ? "active" : "")}= link_to "Ranking", ranking_path
        - if current_user.admin?
          %li{:class => (s == "invitation" ? "active" : "")}= link_to "Zaproszenia", new_user_invitation_path
      %ul.nav.navbar-nav.navbar-right
        %li= link_to current_user.username + " - wyloguj się", destroy_user_session_path, :method => "delete"
